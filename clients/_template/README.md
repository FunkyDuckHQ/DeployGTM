# Client Workspace Template

Copy this folder to `clients/{client_id}/` or run:

```text
python 3_operations/scripts/bootstrap_client.py --client {client_id}
```

Replace the placeholder accounts, signals, and scoring weights before running a client workflow.
