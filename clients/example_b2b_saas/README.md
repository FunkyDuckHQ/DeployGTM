# Example B2B SaaS Client Workspace

This fixture proves the workflow is not built around Peregrine or space-sector assumptions.

It uses the same file-based engine with different:

- account inputs
- signal definitions
- ICP scoring weights
- route thresholds

No real vendor integrations are enabled.
